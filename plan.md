1. **Identify the optimization:** The `webServer.cpp` file contains a series of `if/else if` checks to determine the MIME type of a requested file based on its extension (lines 221-238). Inside these checks, `strlen()` is called repeatedly for macros like `HTML_EXT`, `JS_EXT`, `CSS_EXT`, etc. These macros are string literals defined in `globals.h`.
2. **Apply the optimization:** Since these macros are string literals, their length is known at compile-time. We can replace `strlen(MACRO_EXT)` with `(sizeof(MACRO_EXT) - 1)`. This will allow the compiler to evaluate the length, completely eliminating the runtime overhead of traversing the string literal in each branch of the `if/else` chain.
3. **Verify locally:** We will create a small standalone C++ file that tests the modified `if/else` logic with compile-time lengths to ensure it functions identically to the original code. This tests the logic independently of the ESP32 framework.
4. **Complete pre-commit steps:** Run the required pre-commit instructions to ensure everything is in order.
5. **Submit the PR:** Create a PR titled "⚡ Bolt: Use compile-time lengths for file extension comparisons" detailing the performance improvement.
