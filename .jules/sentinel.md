## 2024-03-17 - [CRITICAL] WebDAV handleMove and checkSamePath Null Pointer Dereference and Buffer Overflow
**Vulnerability:** A Denial of Service (DoS) vulnerability via Null Pointer Dereference in `handleMove` and a memory corruption/buffer overflow via invalid pointer arithmetic in `checkSamePath` existed when parsing the `Destination` header or path paths without slashes.
**Learning:** Functions like `strstr` and `strrchr` can return `NULL` if the character/substring is not found. Without `NULL` checks, subsequent pointer arithmetic (`NULL - string`) or `memmove` operations on these pointers cause fatal crashes. Additionally, using `strlen(source_path)` for VLAs inside a function can be unsafe if it is dynamically sized or calculated from unsafe operations.
**Prevention:** Always check pointers returned from string search functions like `strstr` and `strrchr` before dereferencing them. Avoid using Variable-Length Arrays (VLAs) in C++ when safe string manipulation techniques are available.
## 2024-05-24 - [Fix Authorization Bypass on ESP-IDF Webserver Handlers]
**Vulnerability:** Many ESP-IDF httpd handler functions (REST APIs, WebSockets, SSE, WebDAV) were missing `checkAuth()` verification, allowing an attacker to bypass authentication logic intended by the system.
**Learning:** In a C++ embedded webserver like `esp_http_server`, each URL handler needs independent verification of authentication requirements if middleware isn't centrally applying it. Additionally, WebSocket handlers process both HTTP handshakes and raw data frames in the same function. Applying authentication checks blindly at the top of a WebSocket handler corrupts the raw data frame by writing a 401 HTTP response into the socket stream.
**Prevention:** Always verify that every registered route handler implements the required authorization checks. For WebSocket handlers, carefully scope HTTP-level logic (like Auth headers) to the initial HTTP handshake block (`if (req->method == HTTP_GET)`).
## 2024-05-30 - [CRITICAL] Prevent Size_t Underflow When Checking Trailing Slashes
**Vulnerability:** A memory corruption vulnerability existed in `webDav.cpp:handleWebDav` when checking for a trailing slash using `pathName[strlen(pathName) - 1] == '/'`. If `pathName` was completely empty, `strlen(pathName)` evaluated to `0`, and subtracting `1` caused an unsigned integer underflow (to `size_t` max, `0xFFFFFFFF`), leading to out-of-bounds array access and a crash.
**Learning:** Checking the last character of a C-string requires guaranteeing the string is not empty. Without bounds checking on string length, array operations involving `strlen(str) - 1` are fundamentally unsafe.
**Prevention:** Always assert `strlen(str) > 0` before attempting to access `str[strlen(str) - 1]`.

## 2024-05-30 - [HIGH] Buffer Overflow in JSON Network List Building
**Vulnerability:** In `webServer.cpp:setupHandler`, `sprintf` was iteratively appending discovered Wi-Fi network information into `jsonBuff` using a `for` loop. Since no bounds checks were performed during iteration, a large number of Wi-Fi networks could exceed `JSON_BUFF_LEN`, overflowing the heap-allocated buffer.
**Learning:** Even if a buffer seems large (`JSON_BUFF_LEN` is 32KB), dynamically sized data from external environments (like nearby Wi-Fi SSIDs) must be bounded. `sprintf` provides no safety for this, and simply tracking a pointer `p` is insufficient without tracking remaining space.
**Prevention:** Iteratively appending strings into a shared buffer requires tracking remaining space. Use `snprintf` combined with remaining space subtraction (`rem -= n`) inside the loop body to ensure total appended data never exceeds buffer size.
## 2024-05-24 - Buffer Overflow in WebDAV header extraction
**Vulnerability:** A local stack buffer `char value[10]` in `webDav.cpp` `handleProp` was used to store the parsed value of the `Depth` HTTP header using `extractHeaderVal`. However, `extractHeaderVal` expects a destination buffer of size `IN_FILE_NAME_LEN` (128 bytes) and extracts up to `IN_FILE_NAME_LEN - 1` bytes without checking the actual destination buffer size, leading to a stack-based buffer overflow if an oversized `Depth` header is provided by a malicious client.
**Learning:** `extractHeaderVal` is inherently unsafe because it does not accept the destination buffer size as an argument. All buffers passed to `extractHeaderVal` must be sized to at least `IN_FILE_NAME_LEN` to prevent overflows.
**Prevention:** Always ensure that local buffers passed to HTTP extraction functions are correctly sized (in this case, `IN_FILE_NAME_LEN`). Avoid hardcoding small buffer sizes like `10` for dynamically extracted HTTP headers.

## 2024-05-24 - Prevent WebDAV Path Traversal
**Vulnerability:** A path traversal vulnerability was found in `webDav.cpp`. Both the requested path (`pathName`) and move destination (`dest`) were URL-decoded but not validated to prevent `../` sequences, allowing arbitrary file system access outside the designated WebDAV folder via HTTP methods like GET, PUT, MOVE, etc.
**Learning:** Path strings coming from HTTP requests (including headers like "Destination" or URIs) must be sanitized *after* URL decoding to catch URL-encoded directory traversal payloads like `%2e%2e%2f`.
**Prevention:** Always validate user-provided paths for `../` sequences immediately after URL-decoding and before passing them to file system APIs like `STORAGE.open()` or `STORAGE.rename()`.
## 2024-05-30 - [HIGH] Missing Centralized Path Traversal Verification
**Vulnerability:** A path traversal vulnerability was found in `webServer.cpp` and `webDav.cpp`. While some checks existed for `../`, they were not comprehensive and failed to account for variations like `\..`, `/..`, or exact matches `..`, and the checks were duplicated manually in various areas making it prone to errors.
**Learning:** Security checks that are duplicated and not comprehensive can easily be bypassed and are difficult to maintain. By creating a centralized utility function (`isPathTraversal`), all variations of a traversal attempt can be validated in one location.
**Prevention:** Use the centralized `isPathTraversal` utility (declared in `stringUtils.h`, defined in `stringUtils.cpp`) to validate file paths and query parameters against directory traversal attacks. This function must be called *after* URL decoding to catch traversal sequences like `..`, `../`, `..\`, `/..`, and `\..` in all web handlers (e.g., `webServer.cpp`, `webDav.cpp`).
## 2024-05-24 - Path Traversal Vulnerability in Upload Handler
**Vulnerability:** In `webServer.cpp`, the `uploadHandler` function checked for path traversal using `isPathTraversal(inFileName)` BEFORE calling `urlDecode` on the path.
**Learning:** URL decoding must occur before checking for path traversal sequences, as encoded payloads (e.g., `%2e%2e%2f`) can easily bypass checks performed on the encoded string.
**Prevention:** Always apply `urlDecode()` to user-provided paths and filenames before validating them against path traversal or other malicious patterns.
## 2024-05-30 - [CRITICAL] Buffer Copy Over-Read/Write in WebDAV handleMove via `strlen(dest)`
**Vulnerability:** In `webDav.cpp:handleMove`, when removing the `/webdav` prefix from the destination path, `memmove(dest, pos + strlen(WEBDAV), strlen(dest))` was used. Using `strlen(dest)` as the size parameter for a `memmove` operation where `pos + strlen(WEBDAV)` is the source copies `strlen(dest)` bytes from a location further down the string, resulting in an over-read past the null terminator of the source string, and overwriting `dest` with garbage data.
**Learning:** In C/C++, when using `memmove`, `memcpy`, or similar functions to copy strings, never use `strlen(dest)` (the length of the destination buffer or the original unshifted string) as the size argument when copying a suffix of the string. This leads to undefined behavior, incomplete copies, or out-of-bounds reads/writes.
**Prevention:** Always base the size argument on the source string length (e.g., `strlen(pos + strlen(WEBDAV)) + 1` to include the null terminator).
## 2024-05-30 - [CRITICAL] Out-of-Bounds Read in WebDAV handleMove via Incorrect memmove Size
**Vulnerability:** In `webDav.cpp:handleMove`, an incorrect size argument was used in a `memmove` operation: `memmove(dest, pos + strlen(WEBDAV), strlen(dest))`. The size argument `strlen(dest)` refers to the length of the entire destination buffer, which is much larger than the remaining source string starting at `pos + strlen(WEBDAV)`. This causes an out-of-bounds read past the end of the source string, potentially copying garbage memory into the destination and causing memory corruption, crashes, or data leakage.
**Learning:** Using the length of a destination buffer as the copy size for string manipulation is a fundamental error. When copying a string from a source to a destination, the length must always be bounded by the source string length (including the null terminator if needed), not the destination length.
**Prevention:** In C/C++, when using `memmove` or `memcpy` to copy strings, always base the size argument on the source string length (e.g., `strlen(source_ptr) + 1` to include the null terminator). Never use `strlen(dest)` as the size argument.
## 2024-06-13 - [Buffer Underflow in parseJson]
**Vulnerability:** A critical out-of-bounds write (heap buffer underflow) was found in `webServer.cpp`'s `parseJson` function. The code executed `jsonBuff[rxSize - 1] = ',';` without validating if `rxSize` was greater than 0.
**Learning:** This existed because the developer assumed that `rxSize` (derived from `req->content_len`) would always be positive. However, an empty request or a socket error could result in a 0 or negative length, leading to a write at index -1 of the global `jsonBuff`, corrupting adjacent memory.
**Prevention:** When writing to buffers using offsets based on dynamic or untrusted size variables, always explicitly validate that the size is greater than zero (`if (size <= 0) return error;`).
## 2024-05-30 - [CRITICAL] Buffer Overflow in URL Encoding
**Vulnerability:** In `utils.cpp:urlEncode`, the `encodedLen` variable was used to track the length of the encoded string to prevent buffer overflows. However, it was only incremented when characters *required* encoding (`encodedLen += 3`). For unencoded characters, `encodedLen` was not incremented, leading to silent, unbound buffer overflows when strings consisted primarily of safe characters. Additionally, `*inVal` was cast incorrectly or not cast before bitwise operations, potentially leading to sign-extension issues with non-ASCII characters indexing into `hexTable`. Finally, the bounds check `if (encodedLen >= maxSize)` failed to account for the space required by the final null byte.
**Learning:** In custom string encoding/escaping loops, ensure the length tracking variable is incremented for *both* encoded/escaped characters and raw/unmodified characters. Also, buffer overflow size checks explicitly account for the space required by the trailing null byte. In C/C++, when processing string characters, always cast `char` variables to `(unsigned char)` before passing them to bitwise operations (e.g., `>> 4`) to prevent sign-extension bugs with non-ASCII data.
**Prevention:** Increment length trackers consistently, explicitly account for null bytes in size limits (e.g., `if (len + 1 > max)`), and explicitly cast characters to `unsigned char` prior to bit manipulation.
## 2024-05-24 - [Fix Basic Auth verification and Out-of-bounds Read]
**Vulnerability:** The `checkAuth` method in `webServer.cpp` blindly added +1 to the `Authorization` header length (even when absent, causing uninitialized read) and validated basic credentials using a loose `strstr(auth, encode64(credentials))`, allowing potential authentication bypasses if the encoded string were embedded in another structure.
**Learning:** Adding 1 to a 0 length creates a 1 byte array that then fails to be populated but is read by a string function. Furthermore, loose authentication checks like `strstr` are inherently flawed for security tokens since they don't ensure the token matches exactly and fully, only that it is contained.
**Prevention:** Always verify header length > 0 before allocating stack memory and reading into it. Always validate security tokens using exact strict matches (e.g. `strcmp`) against the full expected value format, including prefixes (like "Basic ").
## 2024-06-15 - [HIGH] CORS Preflight Authentication Bypass
**Vulnerability:** In `webDav.cpp:handleWebDav`, `checkAuth()` was called before handling `HTTP_OPTIONS` requests. Because CORS preflight `OPTIONS` requests do not include an `Authorization` header, the authentication check failed, sending a `401 Unauthorized` and breaking cross-origin resource sharing (CORS) intended by the developer. Furthermore, handling `HTTP_OPTIONS` in `webServer.cpp:customOrNotFoundHandler` but not returning early caused a double-response send.
**Learning:** In ESP-IDF `httpd` handlers (e.g., `handleWebDav`), always handle `HTTP_OPTIONS` requests for CORS preflights *before* calling `checkAuth()`. Preflight requests are unauthenticated by design. Also, ensure early returns when sending HTTP headers for `OPTIONS` to prevent double-sends in subsequent routing.
**Prevention:** Intercept `HTTP_OPTIONS` at the very beginning of the endpoint handler (or before invoking authentication logic), apply the required CORS headers, and return early.

## 2024-04-14 - Fix Buffer Overflow Risk in HTTP Query Parsing
**Vulnerability:** A classic buffer overflow risk existed in `webServer.cpp` where `strcpy(value, variable + strlen(variable) + 1)` was used to extract query parameter values into a statically sized `value` buffer (`IN_FILE_NAME_LEN`).
**Learning:** In resource-constrained C/C++ environments, developers sometimes use unsafe string functions like `strcpy` for convenience or perceived performance, overlooking the risk of untrusted HTTP input exceeding buffer limits.
**Prevention:** Always use bounded string copy functions like `strncpy(dest, src, max_size)` and immediately followed by `dest[max_size - 1] = '\0'` to guarantee null-termination, or better yet, use robust string classes or `snprintf` if applicable.
## 2024-06-25 - [CRITICAL] Buffer Overflow in extractKeyVal due to unbounded strcpy
**Vulnerability:** A critical buffer overflow vulnerability was found in `appSpecific.cpp` within the `extractKeyVal` function used to parse WebSocket messages. A `strcpy(value, variable + strlen(variable) + 1)` was used to copy the second part of a query string directly into a statically sized `value` buffer (`FILE_NAME_LEN`). Because WebSocket message length was unbounded or larger than `FILE_NAME_LEN`, this permitted buffer overflow.
**Learning:** Functions that parse untrusted inputs such as WebSocket payloads must always use bounded string copying operations (`strncpy`, `snprintf`), and explicit bounds checking.
**Prevention:** In functions copying strings derived from inputs (e.g., query strings, WebSocket messages) into fixed-size arrays, always use `strncpy(dest, src, size - 1)` followed by explicit null termination `dest[size - 1] = 0`.

## 2024-05-24 - [Unsafe Manual String Extraction]
**Vulnerability:** In `webServer.cpp`, `controlHandler` attempted to manually extract a URL query value using `strncpy(value, variable + strlen(variable) + 1, ...)`. If an HTTP request contained a query parameter without an equals sign delimiter, `strlen` covered the entire string, causing the pointer arithmetic `+ 1` to index past the null terminator into uninitialized stack memory, resulting in an Out-Of-Bounds (OOB) read.
**Learning:** Manual C-string manipulation with pointer arithmetic past null terminators is extremely fragile and error-prone, especially when mirroring the functionality of a dedicated extraction function (`extractQueryKeyVal`) that already safely processes the input and buffers.
**Prevention:** In C/C++, avoid extracting substring values using pointer arithmetic like `buffer + strlen(buffer) + 1` (expecting it to point after a delimiter). If the expected delimiter is missing, this points past the null terminator into uninitialized memory, leading to an Out-Of-Bounds (OOB) read vulnerability. Rely on robust parsing functions instead.
## 2024-05-30 - [MEDIUM] Redundant Pointer Arithmetic Extraction Causes OOB Risk and Performance Loss
**Vulnerability:** In `webServer.cpp` and `appSpecific.cpp`, the codebase extracted substrings by calling `strlen` on a buffer that just had a null-terminator inserted via `strchr` (`variable + strlen(variable) + 1`), and sometimes repeated this extraction redundantly after `extractQueryKeyVal` had already populated the output buffers.
**Learning:** Using `variable + strlen(variable) + 1` requires O(N) traversal and is less robust than directly using `endPtr + 1` returned by `strchr`. Redundantly repeating this logic after a utility function has already completed the task introduces Out-of-Bounds (OOB) read risks and code duplication.
**Prevention:** When splitting strings in-place with `strchr`, always use the resulting pointer (e.g., `endPtr + 1`) to calculate the remainder offset. Do not manually extract values immediately after calling utility functions like `extractQueryKeyVal` that already perform the extraction safely.
## 2026-05-14 - Array Out-Of-Bounds in utilsFS.cpp
**Vulnerability:** Array out-of-bounds read vulnerability when accessing `fsTypes` and `fsPaths` using `thisFS` when `thisFS` defaults to `TBD`.
**Learning:** Hardcoded mapping arrays matching an enum need to be the same length as the enum values used to access them, including the default or error states.
**Prevention:** Extend arrays to cover all enum variants, and use tools like `cppcheck` continuously.

## 2024-06-25 - [CRITICAL] Stack Buffer Overflow in downloadAvi via Incorrect memmove Size
**Vulnerability:** In `appSpecific.cpp:downloadAvi`, an incorrect size argument was used in a `memmove` operation: `memmove(pos, fileName, sizeof(fileName) - (pos - fileName))`. The size argument incorrectly used the remaining size of the destination buffer rather than the length of the source string. This caused an out-of-bounds read past the null terminator of the source string into uninitialized stack memory, copying garbage data. The resulting buffer lacked a null terminator where `strncat` expected one, further triggering an underflow in `strncat` length limits and causing stack corruption.
**Learning:** Using the remaining space of a destination buffer as the copy length for string manipulation is a classic buffer over-read/overflow vulnerability.
**Prevention:** Always base `memmove` and `memcpy` lengths on the source string length (e.g. `strlen(src) + 1`), bounded by the destination size. Alternatively, use `snprintf` to safely format strings without manual pointer arithmetic.

## 2024-07-15 - [CRITICAL] NULL Pointer Dereference and Typo in sizeof()
**Vulnerability:** In `telegram.cpp:searchJsonResponse`, there were two vulnerabilities:
1. `char* endItem = strchr(startItem, ',');` was followed directly by `int valSize = endItem - startItem;`. If the JSON did not contain a `,` (e.g., if it was malformed or the last item in the object), `strchr` would return `NULL`. The subsequent pointer arithmetic `NULL - startItem` would underflow, leading to an out-of-bounds memory read (or crash) during the `strncpy` operation.
2. A typo in the bounds check `valSize = sizeof(keyValue - 1);` resulted in taking the size of a pointer (`char*`) rather than the array minus one. This would silently truncate any value longer than 4 (or 8) bytes to 4 (or 8) bytes, breaking functionality.
**Learning:** Never assume string searching functions like `strchr` or `strstr` will successfully find the character/substring in untrusted input like JSON responses. Failing to check for `NULL` before pointer arithmetic is a classic cause of crashes and out-of-bounds memory corruption. Furthermore, typos in `sizeof` parameters can be syntactically valid (due to array decay to pointer) but logically disastrous.
**Prevention:** Always verify the return value of `strchr` or `strstr` is not `NULL` before using it in pointer arithmetic or string operations. Use robust JSON parsing libraries if available, or ensure manual parsing correctly handles edge cases (like the end of an object `}` or the end of the string). Always double-check `sizeof` usage to ensure you are taking the size of the *buffer* and then subtracting, i.e., `sizeof(buffer) - 1`, not `sizeof(buffer - 1)`.
## 2024-11-20 - Path Traversal in WebDAV
**Vulnerability:** The WebDAV implementation in `webDav.cpp` copied `req->uri` directly to `pathName` and performed URL decoding, but did not check for path traversal sequences like `../`. This allowed an attacker to read, write, or delete files outside of the intended WebDAV root folder.
**Learning:** Even when restricting root access physically, input URI strings for file handling protocols like WebDAV must always be sanitized against relative path escalation sequences like `../` to prevent arbitrary file system manipulation.
**Prevention:** Explicitly validate URL-decoded input paths with `strstr(pathName, "../")` before using them in file system operations.
## 2024-05-24 - Path Traversal Vulnerability in webServer.cpp
**Vulnerability:** Path traversal possible by passing `../` in the `variable` parameter of `webServer.cpp`, bypassing intended directory scope after `snprintf` formatting.
**Learning:** Checking for traversal *before* formatting might not be enough or was omitted at the specific usage site where `variable` was processed for static file responses. The requested fix specifically required a `strstr(variable, "../")` check after formatting.
**Prevention:** Ensure explicit bounds and traversal checks (`strstr(..., "../")` or robust alternatives) are placed immediately before accessing files based on user-provided path segments.

## 2024-06-25 - [CRITICAL] Unbounded strcpy Buffer Overflow in appSpecificWebHandler
**Vulnerability:** A critical buffer overflow risk was identified in `appSpecificWebHandler` (`appSpecific.cpp`), where `strcpy(inFileName, value);` was used to copy URL parameter values directly into a statically sized `inFileName` array (`IN_FILE_NAME_LEN`). Because `value` comes from untrusted HTTP queries, it could easily exceed the array bounds.
**Learning:** Functions that parse untrusted input from HTTP queries or WebSocket payloads into fixed-size buffers must always use bounded copying operations like `strncpy` or `snprintf`.
**Prevention:** Always use bounds checking when extracting input: `strncpy(dest, src, size - 1)` followed by explicit null termination `dest[size - 1] = 0`.
## 2024-05-18 - [CRITICAL] Buffer Overflow in tgramAlert due to unsafe pointer math and strncpy
**Vulnerability:** In `appSpecific.cpp`, the `tgramAlert` function extracts a file name from the `subject` string to construct an `alertCaption`. It used `strncpy(alertCaption, pos1, pos2 - pos1)` to copy the string, followed by `alertCaption[pos2 - pos1] = 0;`. If `pos2` was less than `pos1` (e.g., if `/` appeared after `.`), `pos2 - pos1` would become a negative integer, which converts to a huge `size_t` value for `strncpy`. This causes an immediate out-of-bounds write buffer overflow. Even for normal strings, a very long string would still exceed the bounds of the 100 character `alertCaption` buffer.
**Learning:** Performing unsafe pointer math subtraction (e.g., `pos2 - pos1`) without asserting that `pos2 > pos1` is extremely dangerous when passed as a length limit. The subtraction can result in a negative integer that overflows `size_t` (an unsigned type), turning it into a massive copy size causing an out-of-bounds write. Furthermore, using `strncpy` without ensuring that the extracted length actually fits inside the destination buffer can still cause stack overflows.
**Prevention:** Always check `pos2 > pos1` before subtracting pointers to determine string lengths. Always bound-check dynamic strings by using `snprintf(dest, sizeof(dest), "%.*s", (int)len, src)` rather than `strncpy` to guarantee null-termination and prevent buffer overflows.

## 2024-11-20 - [CRITICAL] Buffer Overflow in mqtt remoteQuery
**Vulnerability:** A critical buffer overflow risk was identified in `mqtt.cpp` where `sprintf(remoteQuery, "%.*s", event->data_len, (char*)event->data)` was used to copy an incoming MQTT payload into a statically sized array `remoteQuery` (`FILE_NAME_LEN * 2`). Because the payload length (`event->data_len`) is controlled by the external MQTT broker and could exceed the buffer bounds, a buffer overflow could occur.
**Learning:** External inputs like MQTT payloads should never be copied into bounded arrays without proper length checks. Using `sprintf` for dynamically sized external data is unsafe and allows buffer overflow vulnerabilities.
**Prevention:** Always use safe bounded string functions like `snprintf` when copying dynamically sized inputs into static buffers, passing the buffer's size explicitly using `sizeof(buffer)`.

## 2024-11-20 - [CRITICAL] WebDAV Destination Header Path Traversal
**Vulnerability:** In `webDav.cpp`, the `handleMove` function extracted the `Destination` header into a buffer, performed `urlDecode`, and used the path to perform file operations without validating it against path traversal. An attacker could provide a payload like `Destination: /webdav/../../etc/passwd` or encoded equivalents to move files outside the intended WebDAV root folder. Additionally, `dest[100]` was used to extract a header value that could be larger, although `extractHeaderVal` safely bounds this copy. It is best practice to use `IN_FILE_NAME_LEN` (128) to accommodate maximum file paths safely.
**Learning:** Even when extracting HTTP headers intended for internal use like `Destination` in WebDAV, if the value maps to a file system path, it must be validated against directory traversal attacks (`../`, `..\\`, `/..`, etc.) *after* URL decoding.
**Prevention:** Always apply the centralized `isPathTraversal` utility to any input path (URI or header) that will be used in a file system operation after URL decoding. When extracting headers intended for filenames, use `IN_FILE_NAME_LEN` as the buffer size rather than arbitrary magic numbers.
## 2024-12-07 - [HIGH] Buffer Overflows in sprintf usages
**Vulnerability:** Several instances of `sprintf` were used throughout the codebase to format strings into fixed-size buffers (`partJson` in `utilsFS.cpp`, `outcome` loop in `motionDetect.cpp`, `tarHeader` octal fields in `utilsFS.cpp`, and external API URL paths like `extMslPath` in `periphsI2C.cpp`). Unbounded use of `sprintf` is a classic vector for stack-based buffer overflow vulnerabilities if the format string values (like file paths, UI names, or classifier labels) exceed the expected bounds.
**Learning:** `sprintf` inherently lacks bounds checking on the destination buffer. This vulnerability is especially prominent in loops mapping elements (e.g., categories to probabilities) or when parsing dynamic runtime values like SD card directory names.
**Prevention:** In C/C++, `sprintf` should be aggressively deprecated in favor of `snprintf` which guarantees bounds are respected. When dealing with loops appending to a buffer, track remaining space using `sizeof(buffer) - strlen(buffer)` as the size limit for subsequent `snprintf` calls to prevent silent overflows across multiple iterations.
## 2026-06-03 - [CRITICAL] Stack Buffer Overflow in Header Extraction
**Vulnerability:** A local stack buffer `char value[10]` in `webDav.cpp` was used to store the parsed value of the `Depth` HTTP header using `extractHeaderVal`. However, `extractHeaderVal` expected a destination buffer of size `IN_FILE_NAME_LEN` (128 bytes) and lacked a `valueSize` parameter, extracting up to `IN_FILE_NAME_LEN - 1` bytes without checking the actual destination buffer size. This led to a stack-based buffer overflow if an oversized `Depth` or `Destination` header was provided by a client.
**Learning:** Functions that parse untrusted external inputs like HTTP headers into dynamically sized or stack-allocated buffers must always accept the destination buffer's maximum capacity as an explicit argument. Mismatched buffer assumptions between a generic utility function and its callers are a primary source of stack buffer overflows.
**Prevention:** Always update generic utility functions (e.g., `extractHeaderVal`, `extractQueryKeyVal`) to mandate a `size_t bufferSize` parameter. Ensure all call sites explicitly pass `sizeof(dest_buffer)` to guarantee memory safety.

## 2024-05-24 - [HIGH] Missing Path Traversal Validation on HTTP Headers in WebDAV
**Vulnerability:** In `webDav.cpp`, the `handleMove` function extracted the `Destination` header to determine where to move/rename a file. While it did URL-decode the `dest` buffer, it failed to perform any validation against path traversal sequences (like `../`). Additionally, the primary URI path (`pathName`) in `handleWebDav` only used a weak, easily-bypassed `strstr(pathName, "../")` check instead of the robust, centralized `isPathTraversal` utility.
**Learning:** Security vulnerabilities often hide in secondary inputs like HTTP headers (`Destination`, `Referer`), not just the primary request URI. Furthermore, relying on ad-hoc string checks for path traversal is fragile compared to utilizing dedicated, well-tested security utilities.
**Prevention:** When processing HTTP requests that accept file paths via headers (such as the `Destination` header in WebDAV `MOVE`/`COPY` operations), ensure the extracted header value is explicitly URL-decoded and validated against path traversal (e.g., via `isPathTraversal`) before performing file system operations. Always upgrade weak, ad-hoc security checks to use centralized, robust validation functions.

## $(date +%Y-%m-%d) - Buffer Overflow in mqtt.cpp jsonBuff
**Vulnerability:** A critical heap buffer overflow vulnerability was discovered in `mqtt.cpp`, specifically within the `sendHasEntities` and `sendMqttHasState` functions. The code used `sprintf(p, ...)` and `p += sprintf(p, ...)` to sequentially append data to the global `jsonBuff` without tracking the remaining buffer space. If the appended JSON data exceeded `JSON_BUFF_LEN`, it would silently overwrite adjacent heap memory.
**Learning:** Sequential string concatenation using `sprintf` and pointer arithmetic is intrinsically unsafe unless the remaining buffer space is strictly calculated and enforced. Even with a large buffer, external data or repetitive loops can exceed the bounds.
**Prevention:** Always use bounds-checking formatting functions like `snprintf`, and when tracking offsets, dynamically update the remaining space (e.g., `rem -= written`) and validate the write success before advancing the pointer. A macro like `JSON_APPEND` is a clean way to enforce this behavior repetitively.
## 2025-02-23 - Buffer Overflow in wsAsyncSendJson
**Vulnerability:** A variable-length array (VLA) of fixed heuristic size was used along with `sprintf` to build JSON websocket messages. This permitted inputs (`dataType` or `wsData`) to exceed the calculated buffer bounds due to format specifier logic or unexpected lengths, causing stack buffer overflows. Stack-based VLAs also pose a risk of general stack exhaustion on the ESP32.
**Learning:** `sizeof` heuristic bounds checks (like `strlen(A) + strlen(B) + 30`) are brittle and often underestimate the necessary padding when format specifiers expand the strings, leading to potential buffer overruns during `sprintf` calls.
**Prevention:** Instead of VLAs and `sprintf`, always calculate the exact output length required dynamically by doing a dry-run `snprintf(NULL, 0, ...)` first. Then, use `malloc` to safely allocate memory on the heap and `snprintf` to perform the actual write securely. Always check for `NULL` input before string operations.
## 2024-06-25 - [HIGH] Path Traversal By-pass in extractQueryKeyVal
**Vulnerability:** The `extractQueryKeyVal` function in `webServer.cpp` decoded the entire URL query string using `urlDecode(variable)` before attempting to split the query string into `key` and `value` pairs at the `=` character. An attacker could bypass path traversal checks by URL-encoding the `=` character in the payload (e.g., `key%3Dvalue=../path`). When the entire string was decoded first, the injected `=` sign became part of the string structure, causing the split operation (`strchr(variable, '=')`) to happen at the attacker-controlled location. This allowed malicious payloads like `../` to evade detection logic that assumed specific key-value structures.
**Learning:** URL decoding must never alter the structural delimiters of a parsed format (like `=` and `&` in query strings). If decoding occurs before splitting, encoded control characters become active delimiters, leading to logic bypasses and parsing vulnerabilities.
**Prevention:** Always parse structured input (like query parameters or headers) based on their original delimiters *before* URL-decoding the extracted components. Split first, then decode the individual keys and values separately.

## 2024-05-18 - WebDAV Handler Missing Authentication Verification
**Vulnerability:** The `handleWebDav` function in `webDav.cpp` processed multiple HTTP methods (PUT, PROPFIND, DELETE, etc.) to handle WebDAV file operations but completely lacked a call to `checkAuth()`. This allowed unauthenticated attackers to bypass intended authentication and perform file system operations.
**Learning:** ESP-IDF custom HTTP handlers do not automatically inherit global authentication checks; authentication must be explicitly verified within the handler itself. Furthermore, WebDAV `OPTIONS` requests must be explicitly exempted from this check, as they serve as unauthenticated CORS preflight requests.
**Prevention:** Always explicitly invoke `checkAuth(req)` in any newly created or modified `httpd_uri_t` handler function that performs sensitive operations, ensuring preflight `OPTIONS` requests are safely ignored.
## $(date +%Y-%m-%d) - [HIGH] Buffer Overflows in sprintf usages
**Vulnerability:** Several instances of `sprintf` were used throughout the codebase to format strings into fixed-size buffers (`partJson` in `utilsFS.cpp`, `outcome` loop in `motionDetect.cpp`). Unbounded use of `sprintf` is a classic vector for stack-based buffer overflow vulnerabilities if the format string values exceed the expected bounds.
**Learning:** `sprintf` inherently lacks bounds checking on the destination buffer. This vulnerability is prominent when parsing dynamic runtime values like SD card directory names or stringifying external timestamps. Even seemingly minor API updates to macro definitions can be rejected if code review strictness expects exact bounds preservation.
**Prevention:** In C/C++, `sprintf` should be aggressively deprecated in favor of `snprintf` which guarantees bounds are respected. When resolving bounds issues without explicit requirement to increase buffer size, preserve original macro size definitions (like `FILE_NAME_LEN`) and rely entirely on `snprintf(buf, sizeof(buf), ...)` rather than unprompted structural changes to the array definition itself.

## 2026-07-18 - [CRITICAL] Stack Buffer Overflow in formatHex via Sign Extension
**Vulnerability:** A local stack buffer `char formatted[(inLen * 3) + 1]` in `utilsLog.cpp` was used to store formatted hex strings using `sprintf(formatted + (i*3), "%02x ", inData[i])`. The input variable `inData[i]` was a `const char*` (often a signed `char` in C++). If a character with the high bit set was encountered (representing a negative number), `sprintf` sign-extended the value to an integer, rendering 8 hex characters (e.g., `ffffff80`) instead of the expected 2. This wrote 9 bytes per iteration, vastly exceeding the allocated 3 bytes per element, causing a severe stack-based buffer overflow.
**Learning:** When using `%02x` with `sprintf` or `snprintf` in C/C++, passing an uncast `char` type is extremely dangerous because of implicit integer promotion and sign extension. If the architecture treats `char` as signed, negative values will silently expand far beyond the intended `02` bounds.
**Prevention:** Always explicitly cast character variables to `(unsigned char)` when formatting them as hexadecimal (e.g., `%02x`), and strictly use bounded formatting functions like `snprintf` with a hardcoded length limit instead of unbounded `sprintf`.

## $(date +%Y-%m-%d) - [CRITICAL] Stack and Heap Buffer Overflows in webServer.cpp
**Vulnerability:** Found two critical out-of-bounds write vulnerabilities in `webServer.cpp`. In `wsHandler`, if the WebSocket payload `wsPkt.len` exceeded `MAX_PAYLOAD_LEN`, the application logged an error but still wrote a null terminator `wsMsg[wsPkt.len] = 0;` causing a stack buffer overflow. In `parseJson`, the parameter `rxSize` lacked bounds checking against `JSON_BUFF_LEN`. If it reached the max buffer size, `jsonBuff[rxSize] = 0;` caused a heap buffer overflow.
**Learning:** Checking for boundary conditions and logging an error is insufficient if the code execution is not properly halted. Continuing to process oversized payloads by truncating them is a dangerous anti-pattern that can lead to logic flaws or undefined behavior.
**Prevention:** Always immediately return an error (e.g., `false`, `ESP_FAIL`) when bounds are exceeded. Never silently truncate and continue processing oversized inputs.
## 2024-05-24 - [HIGH] Incomplete Path Traversal Validation After String Formatting
**Vulnerability:** A path traversal vulnerability existed in `webServer.cpp` due to checking for `../` on an input string `variable` *after* the input was formatted into a final file path `inFileName` via `snprintf`. Because `snprintf` truncated the path to `IN_FILE_NAME_LEN`, there could theoretically be a scenario where truncation created a bypass, or more commonly, verifying the input instead of the final formatted artifact allows bypasses if the formatting process itself introduces structural changes.
**Learning:** Security validation must always be performed on the final formatted string that will be used for file system operations, not the intermediate components, especially if length truncation or other transformations are involved. Furthermore, relying on an ad-hoc `strstr` check for `../` is less comprehensive than using the established `isPathTraversal` utility.
**Prevention:** Always validate the completely formatted and potentially truncated string using a robust, centralized utility like `isPathTraversal` before utilizing it in any sensitive operation. Never rely on checking intermediate variables once they have been combined or formatted into a new string.
## 2024-07-27 - [HIGH] Buffer Overflow in prefs.cpp JSON builder
**Vulnerability:** In `prefs.cpp`, the `buildJsonString` function repeatedly used `sprintf` to append configuration strings and passwords into the `jsonBuff` buffer. Because there was no tracking of the remaining buffer size during these string concatenations, a heap-based buffer overflow could easily occur if the configuration strings exceeded the allocated size.
**Learning:** Even if a buffer like `jsonBuff` is large, chaining unbounded `sprintf` calls inside a function or a loop is a classic vector for memory corruption. The destination buffer is completely blind to its own limits.
**Prevention:** In C/C++, use a macro (like `JSON_APPEND`) or a standardized helper function that utilizes `snprintf` combined with a remaining space tracker (`size_t rem`). Ensure the tracker is correctly decremented and the write pointer is correctly advanced *only if* the write succeeded within bounds.

## 2024-07-27 - [CRITICAL] Buffer Overflow in retrieveConfigVal due to unbounded strcpy
**Vulnerability:** A critical buffer overflow risk existed in `prefs.cpp` where `strcpy(value, configs[keyPos][1].c_str())` was used to copy configuration strings into a statically sized `value` buffer. This function was called across different parts of the code with varying sized destination buffers (e.g., `appId[16]`, `fsizePtrStr[4]`). Since configuration values from flash/SD are essentially untrusted input, a maliciously crafted config file could overflow these local arrays on the stack or BSS, potentially achieving remote code execution.
**Learning:** Centralized configuration retrieval functions (`retrieveConfigVal`) must always enforce buffer bounds, because the size of the destination buffer is not known at the function definition site and changes depending on the caller context.
**Prevention:** Always mandate a `size_t bufferSize` parameter for configuration retrieval functions and use bounded string operations (`strncpy` followed by explicit null termination) to ensure the destination buffer is never overflowed, regardless of how large the stored configuration value is.
## 2024-06-25 - [HIGH] Buffer Overflow in mqtt_uri
**Vulnerability:** In `mqtt.cpp`, an unbounded `sprintf` was used to concatenate `mqtt_broker` and `mqtt_port` into `mqtt_uri`. While `mqtt_uri` was correctly sized `FILE_NAME_LEN`, `sprintf` does not inherently enforce boundaries.
**Learning:** `sprintf` inherently lacks bounds checking on the destination buffer. External inputs like MQTT payloads should never be copied into bounded arrays without proper length checks.
**Prevention:** In C/C++, `sprintf` should be aggressively deprecated in favor of `snprintf` which guarantees bounds are respected.

## 2024-08-05 - [HIGH] Buffer Overflows in MQTT and RTSP String Formatting
**Vulnerability:** Found buffer overflow vulnerabilities in `mqtt.cpp` and `rtsp.cpp`. In `mqtt.cpp`, an array `mqtt_uri[FILE_NAME_LEN]` (64 bytes) was formatted with `sprintf` which combined strings (`mqtt_broker` and `mqtt_port`) that could collectively exceed the buffer size. In `rtsp.cpp`, `char data[100]` was used to combine the current time, FPS, and telemetry data via `sprintf(data + len, "%s", srtBuffer)`. Since `srtBuffer` could be up to `MAX_LINE_LEN` (128 bytes), this inevitably caused stack overflows.
**Learning:** External or configurable inputs (like broker names and telemetry formats) can unpredictably exceed buffer bounds. Static arrays must be large enough to accommodate the worst-case scenario, and `sprintf` inherently lacks the bounds checking required to enforce this limit.
**Prevention:** Always use `snprintf` with bounds limits derived from `sizeof(buffer)` instead of `sprintf`. Furthermore, ensure destination buffers are sized appropriately to hold all appended elements safely.
## 2024-08-14 - Fix Buffer Overflow in listDir jsonBuff construction
**Vulnerability:** The buffer boundary was checked loosely before `strcpy(jsonBuff + buffLen, fileInfo.c_str());`, but `strcpy` is inherently unsafe and can cause buffer overflows if the boundary logic is flawed or edge cases exist.
**Learning:** `strcpy` should never be used, even if prior length checks seem sufficient, as it lacks inherent bounds checking and depends entirely on the surrounding logic to prevent overwrites.
**Prevention:** Always use safe, bounds-checked formatting functions like `snprintf(dest, size, "%s", src)` or `strncpy(dest, src, size)` instead of `strcpy` to guarantee proper bounds checking and null-termination regardless of prior logic.
